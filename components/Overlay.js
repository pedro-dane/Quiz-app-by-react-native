import React from 'react'
import {View} from 'react-native'

const Overlay = (props) => (
 <View style={{width: '100%', backgroundColor: 'red', minWidth:'100%', minHeight: '100%', position:'absolute', top:0, left: 0, right: 0}}>
  </View>
)

export default Overlay